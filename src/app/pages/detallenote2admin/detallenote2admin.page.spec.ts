import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Detallenote2adminPage } from './detallenote2admin.page';

describe('Detallenote2adminPage', () => {
  let component: Detallenote2adminPage;
  let fixture: ComponentFixture<Detallenote2adminPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Detallenote2adminPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
